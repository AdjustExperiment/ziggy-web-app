name: Build & Deploy (FTPS → releases/current with diagnostics)

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      BUILD_ID:
        description: "Release folder to roll back to (e.g. 20251018T151410Z)"
        required: false
        type: string

jobs:
  build-and-deploy:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      # 🧩 Checkout code
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # 🟢 Setup Node
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 📦 Install dependencies (legacy peer deps)
      - name: Install deps (legacy peer deps)
        run: npm ci --legacy-peer-deps

      # 🔐 Write environment vars for Vite
      - name: Generate .en
